import React from "react";
import { MainContentProps } from "../interface/interfaces";

const MainContent: React.FC<MainContentProps> = ({ children }) => {
  return <>{children}</>;
};

export default MainContent;
